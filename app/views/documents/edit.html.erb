<%= content_for :nav do %>
  <li class="nav-item">
    <%= link_to_unless_current("Edit Document #{@document.id}",
                               { action: :edit, id: @document.id },
                               { class: 'nav-link'}) do %>
      <a class="nav-link active" href="#">
        <%= "Edit Document #{@document.id}" %>
      </a>
    <% end %>
  </li>
<% end %>

<h1 class="mt-3">Editing Document</h1>

<%= render 'form', document: @document %>

<%= link_to 'Show', @document %> |
<%= link_to 'Back', documents_path %>
